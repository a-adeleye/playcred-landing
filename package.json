{
  "name": "playcred-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-landing.cjs production",
    "deploy": "node scripts/build-landing.cjs staging && firebase deploy --only hosting:playcred"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
